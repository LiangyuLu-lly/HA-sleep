# Training-time / dev-only dependencies — NOT shipped in the add-on image.
#
# Use this file on a workstation when you need to:
#   * (re)train the CNN-BiLSTM model from raw EDF recordings
#   * run the full pytest suite (numerical equivalence, contract tests…)
#   * generate evaluation reports / training plots
#
# Install with:   pip install -r requirements-runtime.txt -r requirements-train.txt
#
# The Pi 4B add-on never installs this file: production inference runs
# entirely through the numpy fallback paths.
-r requirements-runtime.txt
tensorflow>=2.13.0
keras>=2.13.0
hypothesis>=6.82.0
pytest>=7.4.0
pytest-asyncio>=0.23.0

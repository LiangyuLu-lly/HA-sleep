# Default dev / CI install — runtime deps plus dev-time test tooling.
#
# Two deployment layers:
#   * requirements-runtime.txt — minimal aiohttp + numpy; shipped in
#     the add-on image (~10 MB of wheels).
#   * requirements.txt (this file) — adds pytest + pytest-asyncio +
#     pytest-timeout for running the test suite on a workstation.
#
# v1.3.0 simplification: there is no longer a ``requirements-train.txt``
# because the local CNN-BiLSTM model + its TensorFlow training pipeline
# were removed in favour of subscribing to an existing HA sleep-stage
# entity.  The Pi 4B add-on installs only ``requirements-runtime.txt``.
-r requirements-runtime.txt
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0

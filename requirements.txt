# 开发 / CI 环境：runtime 依赖 + pytest 三件套。
#
# 两层部署：
#   * requirements-runtime.txt —— Add-on 镜像只装这一份（目前只有
#     aiohttp；v1.6.0 已移除 numpy）。
#   * requirements.txt（本文件）—— 工作站跑单测用，在 runtime 基础
#     上加 pytest、pytest-asyncio、pytest-timeout。
#
# v1.3.0 起已删除本地 CNN-BiLSTM 模型与配套训练流水线，因此没有
# ``requirements-train.txt``；Alpine Add-on 容器只安装
# ``requirements-runtime.txt``。
-r requirements-runtime.txt
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0

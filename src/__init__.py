# Sleep Classifier — runtime modules.
#
# Every concrete module lives directly under ``src/`` (no sub-packages).
# ``scripts/run_ha_smart_service.py`` is the single asyncio entrypoint
# that wires them together; see ``.kiro/steering/structure.md`` for the
# per-module responsibility table.
